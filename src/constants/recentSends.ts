/**
 * Constants for recent sends functionality
 */

const DEFAULT_DATE_RANGE_DAYS = 30;
const PAGE_SIZE = 5;
const POLLING_DELAY_MS = 10000;

export { DEFAULT_DATE_RANGE_DAYS, PAGE_SIZE, POLLING_DELAY_MS };
